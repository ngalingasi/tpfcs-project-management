meta {
  name: Change Password
  type: http
  seq: 5
}

post {
  url: {{base_url}}/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": "password",
    "newPassword": "password"
  }
}
