meta {
  name: Update Objective
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/objectives/1
  body: json
  auth: bearer
}
auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "title": "Increase clean water access to 90%",
    "status": "in_progress"
  }
}
