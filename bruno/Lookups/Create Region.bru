meta {
  name: Create Region
  type: http
  seq: 6
}

post {
  url: {{base_url}}/lookups/regions
  body: json
  auth: bearer
}
auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "region_name": "Dar es Salaam"
  }
}
